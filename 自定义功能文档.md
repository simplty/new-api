# 自定义功能文档

本文档记录了在开发过程中对代码的所有自定义修改和功能增强。

## 修改记录

### 2025-07-15 - 导航栏文本修改

**修改描述**: 将前端导航栏中的"定价"改为"模型列表"

**修改文件**:
1. `web/src/components/layout/HeaderBar.js:108`
   - 将 `text: t('定价')` 改为 `text: t('模型列表')`

2. `web/src/i18n/locales/zh.json:4`
   - 将 `"定价": "定价"` 改为 `"模型列表": "模型列表"`

3. `web/src/i18n/locales/en.json:956`
   - 将 `"定价": "Pricing"` 改为 `"模型列表": "Model List"`

**影响范围**: 
- 前端导航栏显示文本
- 中英文国际化翻译
- 页面路由和功能保持不变（仍然指向 `/pricing` 路径）

**备注**: 这个修改只是界面文本的更新，底层的定价功能和页面内容没有改变。