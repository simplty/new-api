# 自定义功能文档

本文档记录了在开发过程中对代码的所有自定义修改和功能增强。

## 修改记录

### 2025-07-15 - 导航栏文本修改

**修改描述**: 将前端导航栏中的"定价"改为"模型列表"

**修改文件**:
1. `web/src/components/layout/HeaderBar.js:108`
   - 将 `text: t('定价')` 改为 `text: t('模型列表')`

2. `web/src/i18n/locales/zh.json:4`
   - 将 `"定价": "定价"` 改为 `"模型列表": "模型列表"`

3. `web/src/i18n/locales/en.json:956`
   - 将 `"定价": "Pricing"` 改为 `"模型列表": "Model List"`

**影响范围**: 
- 前端导航栏显示文本
- 中英文国际化翻译
- 页面路由和功能保持不变（仍然指向 `/pricing` 路径）

**备注**: 这个修改只是界面文本的更新，底层的定价功能和页面内容没有改变。

### 2025-07-17 - 令牌创建默认值优化

**修改描述**: 优化令牌创建流程，改善用户体验，设置更合理的默认值

**功能需求**:
1. 令牌分组默认为 `auto`（智能熔断）
2. 额度设置默认为无限额度

**当前默认值分析**:
- 额度默认值: 500,000（相当于1美元）
- 过期时间: -1（永不过期）
- 分组: 空字符串（使用用户默认分组）
- 模型限制: 未启用，支持所有模型
- IP限制: 无限制
- 状态: 启用（1）
- 无限额度: 否（false）

**需要修改的文件**:
1. `web/src/pages/Token/EditToken.js` - 修改前端表单默认值
   - `group: 'auto'` （从空字符串改为'auto'）
   - `unlimited_quota: true` （从false改为true）

**变更影响**:
- 新创建的令牌将自动使用智能熔断功能
- 新创建的令牌将默认为无限额度，减少用户配置步骤
- 提升用户体验，减少手动配置工作

**实际修改**:
1. `web/src/pages/Token/EditToken.js:55`
   - 将 `group: ''` 改为 `group: 'auto'`
   
2. `web/src/pages/Token/EditToken.js:51`
   - 将 `unlimited_quota: false` 改为 `unlimited_quota: true`

**修改完成时间**: 2025-07-17

**测试状态**: 已验证

**验证结果**:
- 前端代码构建成功 ✓
- 后端代码构建成功 ✓
- 默认值修改已正确应用 ✓

**功能验证**:
- 令牌分组默认值从空字符串改为'auto' ✓
- 无限额度默认值从false改为true ✓
- 代码编译无错误 ✓